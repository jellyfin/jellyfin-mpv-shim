"""Vacuity sweep -- generated by tools/audit_vacuous_narrowing.py.

Every mutation below disables one narrowing: a query scope stops scoping, or a
membership gate stops firing. **A survivor is a narrowing no test can tell
apart from its own absence.**

Do not read a survivor as a bug on its own. It is one of two things and this
plan cannot say which: a filter that never filtered (the defect), or a real
filter nobody tests (the gap). Both want a person. What a survivor does rule
out is "the suite covers this", which is what made the original defect of this
shape survive four review rounds.

Regenerate rather than edit -- line numbers move.
"""

SELECT = []

MUTATIONS = [
    ('membership-gate jellyfin_mpv_shim/books.py:112 (against PAGED_FORMATS)',
     'jellyfin_mpv_shim/books.py',
     '    if fmt in PAGED_FORMATS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/books.py:114 (against PERCENT_FORMATS)',
     'jellyfin_mpv_shim/books.py',
     '    if fmt in PERCENT_FORMATS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/conf.py:929 (against _NOTIFY_STATES)',
     'jellyfin_mpv_shim/conf.py',
     '            if not isinstance(raw, str) or raw not in _NOTIFY_STATES:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/event_handler.py:216 (against NAVIGATION_DICT)',
     'jellyfin_mpv_shim/event_handler.py',
     '        elif command in NAVIGATION_DICT:',
     '        elif False:'),
    ('membership-gate jellyfin_mpv_shim/keysweep.py:76 (against _PROPERTY_VERBS)',
     'jellyfin_mpv_shim/keysweep.py',
     '    if verb in _PROPERTY_VERBS and len(parts) >= 2:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/log_utils.py:173 (against RING_EXCLUDED)',
     'jellyfin_mpv_shim/log_utils.py',
     '            if record.name in RING_EXCLUDED:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/media.py:933 (against _SERVER_CONVERTED_IMAGES)',
     'jellyfin_mpv_shim/media.py',
     '            if (container in _SERVER_CONVERTED_IMAGES or any(\n                    path.endswith("." + ext)\n                    for ext in _SERVER_CONVERTED_IMAGES)\n                    or not may_download(self.client)):',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/menu.py:525 (against HEX_TO_COLOR)',
     'jellyfin_mpv_shim/menu.py',
     '        if color in HEX_TO_COLOR:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpv_options.py:144 (against _HWDEC_STATIC)',
     'jellyfin_mpv_shim/mpv_options.py',
     '    if mode in _HWDEC_STATIC:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpv_options.py:547 (against REPLACES_OSC)',
     'jellyfin_mpv_shim/mpv_options.py',
     '    if osc_style in REPLACES_OSC:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/player.py:4967 (against REPLACES_OSC)',
     'jellyfin_mpv_shim/player.py',
     '            if style in REPLACES_OSC and hasattr(self._player, "osc"):',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/restart.py:170 (against _PYI_MARKERS)',
     'jellyfin_mpv_shim/restart.py',
     '        if name in _PYI_MARKERS or name.startswith(_PYI_PREFIX):',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/system_open.py:133 (against URL_SCHEMES)',
     'jellyfin_mpv_shim/system_open.py',
     '    if parts.scheme.lower() not in URL_SCHEMES or not parts.netloc:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/tray.py:205 (against _NATIVE_BACKENDS)',
     'jellyfin_mpv_shim/tray.py',
     '    if backend in _NATIVE_BACKENDS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/tray.py:210 (against _XEMBED_BACKENDS)',
     'jellyfin_mpv_shim/tray.py',
     '    if backend in _XEMBED_BACKENDS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/tray.py:212 (against _SNI_BACKENDS)',
     'jellyfin_mpv_shim/tray.py',
     '    if backend in _SNI_BACKENDS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/tray.py:464 (against _SNI_BACKENDS)',
     'jellyfin_mpv_shim/tray.py',
     '        if backend not in _SNI_BACKENDS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/update_check.py:110 (against ALLOWED_OWNERS)',
     'jellyfin_mpv_shim/update_check.py',
     '    if owner.lower() not in ALLOWED_OWNERS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/update_check.py:149 (against _REDIRECTS)',
     'jellyfin_mpv_shim/update_check.py',
     '            if response.status_code not in _REDIRECTS:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/utils.py:77 (against _AUDIO_CONTAINERS)',
     'jellyfin_mpv_shim/utils.py',
     '    if item.get("Type") in _AUDIO_CONTAINERS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/video_profile.py:293 (against NAIVE_HWDEC)',
     'jellyfin_mpv_shim/video_profile.py',
     '                if str(value).strip().lower() in NAIVE_HWDEC:',
     '                if False:'),
    ('sql-scope jellyfin_mpv_shim/sync/db.py list_playlists',
     'jellyfin_mpv_shim/sync/db.py',
     '        else:\n            scope = " AND (p.server_id=? OR p.server_id IS NULL)"\n',
     '        elif False:\n            scope = " AND (p.server_id=? OR p.server_id IS NULL)"\n'),
    # The claim this one carries is the whole of step 2's convention change:
    # a scope of None means "a login that resolves to no server" and must
    # match nothing. Put back the old reading -- None runs unscoped -- and
    # something has to notice, or the split bought nothing.
    ('sql-scope jellyfin_mpv_shim/sync/db.py _content_clause None',
     'jellyfin_mpv_shim/sync/db.py',
     '        if not server_id:\n            # Not `content_server_id IS NULL`',
     '        if not server_id:\n            return "", tuple(first)\n            # Not `content_server_id IS NULL`'),
    # `clear_discarded` used to guard its scoped delete behind `if server_id:`
    # and delete the unscoped row unconditionally. There is one table now, so
    # the guard is the early return and the claim is the same: a clear names
    # one server's tombstone and never every server's.
    ('sql-scope jellyfin_mpv_shim/sync/db.py clear_discarded',
     'jellyfin_mpv_shim/sync/db.py',
     '                self._conn.execute(\n                    "DELETE FROM auto_discarded_scoped "\n                    "WHERE item_id=? AND server_id=?",\n                    (item_id, server_id))',
     '                self._conn.execute(\n                    "DELETE FROM auto_discarded_scoped "\n                    "WHERE item_id=?",\n                    (item_id,))'),
    ('membership-gate jellyfin_mpv_shim/sync/manager.py:1684 (against FOLDER_ITEM_TYPES)',
     'jellyfin_mpv_shim/sync/manager.py',
     '            if item_type in FOLDER_ITEM_TYPES:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/sync/manager.py:2139 (against RESERVED_STORE_DIRS)',
     'jellyfin_mpv_shim/sync/manager.py',
     '            if child in RESERVED_STORE_DIRS or child in known:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:676 (against USERDATA_KINDS)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if self.route.get("kind") not in USERDATA_KINDS or not self._browsing:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:1470 (against CHROME_FREE)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if route.get("kind") not in CHROME_FREE:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:1477 (against NO_NOW_PLAYING)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if (self._now_playing is not None\n                and route.get("kind") not in NO_NOW_PLAYING):',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2916 (against LIVE_KINDS)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if route["kind"] in LIVE_KINDS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2925 (against CHROME_FREE)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if route["kind"] not in CHROME_FREE:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2939 (against NO_NOW_PLAYING)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        if (self._now_playing is not None\n                and route["kind"] not in NO_NOW_PLAYING):',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2130 (against LIVE_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        elif t in LIVE_TYPES:',
     '        elif False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2148 (against SERIES_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        elif t in SERIES_TYPES:',
     '        elif False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2158 (against PLAYABLE_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        elif t in PLAYABLE_TYPES:',
     '        elif False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/app.py:2162 (against FOLDER_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/app.py',
     '        elif t in FOLDER_TYPES or item.get("CollectionType"):',
     '        elif False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/config.py:1091 (against LABEL_OVERRIDES)',
     'jellyfin_mpv_shim/mpvtk_browser/config.py',
     '    if key in LABEL_OVERRIDES:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/config.py:1156 (against EXCLUSIVE_PLATFORMS)',
     'jellyfin_mpv_shim/mpvtk_browser/config.py',
     '    if sys.platform in EXCLUSIVE_PLATFORMS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/config.py:1340 (against _HIDDEN)',
     'jellyfin_mpv_shim/mpvtk_browser/config.py',
     '        if key.startswith("_") or key in _HIDDEN:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/navigator.py:113 (against LOCKED_ROUTES)',
     'jellyfin_mpv_shim/mpvtk_browser/navigator.py',
     '        if self._is_locked() and kind not in LOCKED_ROUTES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/navigator.py:116 (against HEADLESS_ROUTES)',
     'jellyfin_mpv_shim/mpvtk_browser/navigator.py',
     '        if self._is_headless() and kind not in HEADLESS_ROUTES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/repository.py:2629 (against GRID_IMAGE_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/repository.py',
     '        if image_type in GRID_IMAGE_TYPES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/repository.py:599 (against EXCLUDED_COLLECTION_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/repository.py',
     '            if item.get("CollectionType") in EXCLUDED_COLLECTION_TYPES:',
     '            if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/themes.py:199 (against _ENUMS)',
     'jellyfin_mpv_shim/mpvtk_browser/themes.py',
     '    if key in _ENUMS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/themes.py:203 (against _COLOUR_KEYS)',
     'jellyfin_mpv_shim/mpvtk_browser/themes.py',
     '    if key in _COLOUR_KEYS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/themes.py:209 (against _GRADIENT_KEYS)',
     'jellyfin_mpv_shim/mpvtk_browser/themes.py',
     '    if key in _GRADIENT_KEYS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/themes.py:279 (against DEFAULT)',
     'jellyfin_mpv_shim/mpvtk_browser/themes.py',
     '        if key not in DEFAULT:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/tiles.py:725 (against PLAYABLE_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/tiles.py',
     '        if t in PLAYABLE_TYPES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/view_prefs.py:170 (against IMAGE_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/view_prefs.py',
     '        if value in IMAGE_TYPES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/components/labels.py:34 (against LISTING_TYPES)',
     'jellyfin_mpv_shim/mpvtk_browser/components/labels.py',
     '    if kind not in LISTING_TYPES or item.get("_subtitle") is not None:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/pages/grid.py:397 (against STUDIO_LIBRARIES)',
     'jellyfin_mpv_shim/mpvtk_browser/pages/grid.py',
     '        if route.get("collection_type") in STUDIO_LIBRARIES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk_browser/pages/grid.py:402 (against GENRE_LIBRARIES)',
     'jellyfin_mpv_shim/mpvtk_browser/pages/grid.py',
     '        if route.get("collection_type") in GENRE_LIBRARIES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/app.py:570 (against SCROLL_MODES)',
     'jellyfin_mpv_shim/mpvtk/app.py',
     '        if mode not in SCROLL_MODES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/layout.py:86 (against _NARROW)',
     'jellyfin_mpv_shim/mpvtk/layout.py',
     '    if ch in _NARROW:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/layout.py:88 (against _WIDE)',
     'jellyfin_mpv_shim/mpvtk/layout.py',
     '    if ch in _WIDE:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/pilfont.py:452 (against _EMOJI_CPS)',
     'jellyfin_mpv_shim/mpvtk/pilfont.py',
     '    if cp in _EMOJI_CPS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/pilfont.py:456 (against _SYMBOL_CPS)',
     'jellyfin_mpv_shim/mpvtk/pilfont.py',
     '    if cp in _SYMBOL_CPS:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/pilfont.py:609 (against _JOINERS)',
     'jellyfin_mpv_shim/mpvtk/pilfont.py',
     '        if ord(ch) in _JOINERS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/mpvtk/theme.py:276 (against TOKENS)',
     'jellyfin_mpv_shim/mpvtk/theme.py',
     '        if name not in TOKENS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/content.py:374 (against _INVISIBLE)',
     'jellyfin_mpv_shim/epub/content.py',
     '        if tag in _INVISIBLE:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/content.py:467 (against _HEADINGS)',
     'jellyfin_mpv_shim/epub/content.py',
     '        if tag in _HEADINGS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/content.py:500 (against _BLOCK_TAGS)',
     'jellyfin_mpv_shim/epub/content.py',
     '        if tag in _BLOCK_TAGS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/content.py:557 (against _BULLETS)',
     'jellyfin_mpv_shim/epub/content.py',
     '        if kind in _BULLETS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/css.py:414 (against _KEYWORD_SIZE)',
     'jellyfin_mpv_shim/epub/css.py',
     '    if value in _KEYWORD_SIZE:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/css.py:435 (against _ABSOLUTE_EM)',
     'jellyfin_mpv_shim/epub/css.py',
     '    if unit in _ABSOLUTE_EM:',
     '    if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/css.py:258 (against SHORTHANDS)',
     'jellyfin_mpv_shim/epub/css.py',
     '        if name in SHORTHANDS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/css.py:261 (against USED_PROPERTIES)',
     'jellyfin_mpv_shim/epub/css.py',
     '        if name not in USED_PROPERTIES:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/layout.py:698 (against _BREAK_AFTER)',
     'jellyfin_mpv_shim/epub/layout.py',
     '        if char in _BREAK_AFTER and i - start >= 2 and len(word) - i - 1 >= 3:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/xmlish.py:168 (against VOID_TAGS)',
     'jellyfin_mpv_shim/epub/xmlish.py',
     '        if tag not in VOID_TAGS and len(self._stack) < MAX_DEPTH:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/xmlish.py:176 (against VOID_TAGS)',
     'jellyfin_mpv_shim/epub/xmlish.py',
     '        if _local(tag) not in VOID_TAGS:',
     '        if False:'),
    ('membership-gate jellyfin_mpv_shim/epub/xmlish.py:186 (against VOID_TAGS)',
     'jellyfin_mpv_shim/epub/xmlish.py',
     '        if tag in VOID_TAGS:',
     '        if False:'),
]
